sudo: false
language: node_js
node_js:
  - 12
cache: npm
branches:
  only:
    - master # build main branch only
script:
  - hexo g
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GH_TOKEN
  keep-history: true
  on:
    branch: master
  local-dir: public